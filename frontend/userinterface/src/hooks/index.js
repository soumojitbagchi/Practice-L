/**
 * Hooks Layer — barrel export
 * Orchestration hooks that bridge API ↔ State for the UI.
 */
export { useAuthActions } from './useAuth';
export { useProducts } from './useProducts';
export { useContactForm } from './useContactForm';
